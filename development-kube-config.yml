apiVersion: apps/v1
kind: Deployment
metadata:
  name: excel
  namespace: development
  labels:
    app: excel
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  selector:
    matchLabels:
      app: excel
  template:
    metadata:
      namespace: development
      labels:
        app: excel
    spec:
      containers:
      - name: excel
        image: registry.gitlab.com/pshenmic/ivend/excel:$CIRCLE_SHA1
        imagePullPolicy: Always
        ports:
          - containerPort: 5000
        readinessProbe:
          httpGet:
            path: /api/v1/status
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 5
          successThreshold: 1
          failureThreshold: 3
      imagePullSecrets:
      - name: gitlabregistrycred
